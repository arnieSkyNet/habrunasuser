# habrunasuser
 habrunasuser is a utility that allows a program to be launched as a specified user, even when initiated from root. It uses a two-stage process to securely pass the program name and run it under the desired user’s environment.
